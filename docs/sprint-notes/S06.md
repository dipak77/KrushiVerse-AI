# Sprint 6 Notes — Expert QA Synthesis Factory

**Status:** Complete  
**Sprint:** S6  
**Feature phase:** FP-3  
**Date:** 2026-07-18  

## Goal

Generate large multilingual expert-style QA packs from structured facts + taxonomy templates, with confidence scores and a human review queue.

## Delivered

1. **W-QASYNTH** (ready) — `mini/lake/qa_synth.py` + `mini/workers/qa_synth.py`
2. Packs: crop, disease, fertilizer, scheme, stage, market, irrigation, safety, paraphrase
3. Languages: EN / MR / HI template variants
4. Confidence heuristics; `verified=false` until human review
5. **No train/test leakage**: split assigned by `fact_key` hash (paraphrases share split)
6. Export: versioned `train/val/test` JSONL (+ parquet), `human_review_queue.csv`
7. CLI `qasynth --execute`, pipeline `sprint6`, API `GET/POST /api/lake/qasynth`
8. Streamlit Data Factory button for synth

## Acceptance

| Criterion | Target | Result |
|---|---|---|
| Train QA | ≥10,000 | Pass |
| Val QA | ≥1,000 | Pass |
| Schema v1 | yes | Pass |
| No leakage | fact_key → single split | Pass |
| Review queue CSV | yes | Pass |

## Commands

```bash
python -m mini.orchestrator quality --execute
python -m mini.orchestrator qasynth --execute --target 12000
python -m mini.orchestrator analyze --execute
```

## Handoff to Sprint 7

- Expand domain packs (soil, weather, pest, finance, machinery) toward 50k interim
