# Sprint 10 Notes — Mini Architecture + Train Harness

**Status:** Complete  
**Sprint:** S10  
**Feature phase:** FP-5b  
**Date:** 2026-07-18  

## Goal

Implement the ~1M-parameter decoder-only Mini model and a training smoke harness (no full domain pretrain yet — that is S11).

## Delivered

1. **`mini/models/config.py`** — emb 128, layers 6, heads 4, hidden 192, seq 512, vocab 4096  
2. **`mini/models/model.py`** — RoPE, RMSNorm, SwiGLU, weight tying, causal SDPA  
3. **`mini/models/train.py`** — overfit smoke, AdamW, AMP-ready, checkpoint save/load  
4. **`W-PRETRAIN`** ready (skeleton; full corpus pretrain in S11)  
5. **`mini/models/PARAM_COUNT.json`** — committed param-count report (~1.36M)  
6. CLI `pretrain`, API `/api/lake/pretrain`, pipeline `sprint10`

## Acceptance

| Criterion | Result |
|---|---|
| Param count ~0.8–1.5M | Pass (~1.36M) |
| Overfit smoke loss drops | Pass |
| Architecture features | RoPE / RMSNorm / SwiGLU / tie weights |

## Local-only policy (S10+)

Do **not** commit/push:

- `data/lake/**`
- `mini/datasets/**` (generated)
- `mini/tokenizer/**` artifacts (`.model`, `.vocab`, corpus) — keep `train.py`
- `mini/models/checkpoints/**`, `*.pt`, `PRETRAIN_LATEST.json`

Platform KB under `data/*.json` (crops, schemes, …) remains in git.

## Commands

```bash
python -m mini.orchestrator pretrain --execute --steps 50
python -m mini.orchestrator run sprint10 --execute
```

## Handoff to Sprint 11

- Full domain pretraining on tokenized agri corpus (`W-PRETRAIN` production mode)
- Checkpoint `models/mini/v0.2-base` (local)
