# Sprint 17 Notes — Mini v1.0 Production Beta / Release

**Status:** Complete  
**Sprint:** S17  
**Feature phase:** FP-10  
**Version:** **1.0.0**  
**Date:** 2026-07-19  

## Goal

Ship **KrushiVerseAI Mini v1.0** as a platform component: hardened docs/runbook, RC eval gate, load smoke, published version matrix, scale roadmap decision (10M+), and Section 8 checklist sign-off (with explicit deferrals).

## Delivered

1. **`mini/release/`** — checklist, version matrix, scale roadmap, load smoke, RC gate  
2. **`W-RELEASE`** ready  
3. **`RUNBOOK.md`** — ops, rollback, health checks  
4. CLI `release --execute`  
5. API `GET/POST /api/mini/release`  
6. Pipeline `sprint17` = `W-EVAL` → `W-RELEASE`  
7. Package version **`1.0.0`**

## Acceptance

| Criterion | Result |
|---|---|
| Hardening + runbook | Pass |
| Eval gate on RC | Pass (default profile) |
| Load smoke + p95 latency | Pass |
| Version matrix published | Pass |
| Scale roadmap (10M family) | Pass — `proceed_with_family_scale` |
| §8 checklist sign-off | Pass (must-haves pass/partial; deferred owned) |

## Commands

```bash
python -m mini.orchestrator release --execute
python -m mini.orchestrator run sprint17 --execute
curl -X POST "http://127.0.0.1:8000/api/mini/release?execute=true"
```

## Deferred (explicit owners)

- 1M human-verified QA · Neo4j HA · Vision-language Mini · Multi-GPU 100M+ · K8s HA  

## Program complete

Sprints 0–17 deliver the Mini factory through **v1.0**. Post-v1.0 work follows the scale roadmap (10M → …) reusing the same workers.
