# Sprint 20 Notes — Mini v2-15M Pretrain (v0.6-base)

**Status:** Wiring complete; smoke green; full overnight run = user machine  
**Sprint:** S20  
**Feature phase:** v2-15M  
**Date:** 2026-07-19  

## Goal

Pretrain the **15M** Mini decoder on packed agri text with **fp16 + grad accumulation + gradient checkpointing**, write **`v0.6-base`**.

## Delivered

1. `train_domain_v2` in `mini/models/pretrain.py`
2. `W-PRETRAIN` supports `--variant v2-15M`
3. CLI flags: `--grad-accum`, `--fp16`, `--grad-checkpoint`, `--config`, `--version`
4. Pipeline `sprint20`
5. Smoke tests `tests/test_mini_sprint20.py`
6. Board: `docs/Next 15M Gen Plan/SPRINT_BOARD.md`

## Laptop full run

```bash
python -m mini.orchestrator pretrain --execute --variant v2-15M --version v0.6-base \
  --steps 10000 --batch-size 4 --grad-accum 4 --block-size 1024 \
  --fp16 --grad-checkpoint --lr 3e-4 --seed 42 --eval-every 500
```

## Smoke

```bash
python -m mini.orchestrator pretrain --execute --variant v2-15M --version v0.6-base \
  --steps 40 --batch-size 2 --grad-accum 2 --block-size 64 --grad-checkpoint --seed 42
```

## Next

**S21** — `v0.7-instruct` SFT from `v0.6-base` (assistant-only, safety mix).
