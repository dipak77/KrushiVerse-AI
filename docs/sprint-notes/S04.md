# Sprint 4 Notes — Normalize / Language / Standard Records

**Status:** Complete  
**Sprint:** S4  
**Feature phase:** FP-2 (standard export)  
**Date:** 2026-07-18  

## Goal

Canonical Schema v1 records with language tags, taxonomy normalization, deterministic train/val/test splits, and versioned dataset export (JSONL + parquet).

## Delivered

1. **W-NORMALIZE** — crop/category resolve via taxonomy on processed facts  
2. **W-LANGDETECT** — en / mr / hi / mixed tagging  
3. **W-STANDARD** — emit `StandardRecord` → lake train/val/test + `mini/datasets/versions/`  
4. **W-STANDARDIZE** — convenience pipeline  
5. Parquet export via pandas/pyarrow when available  
6. CLI `standardize --execute`, pipeline `sprint4`  
7. API `GET/POST /api/lake/standard`

## Acceptance

| Criterion | Result |
|---|---|
| ≥90% records have language | Pass |
| ≥90% records have category | Pass |
| Train split exportable (JSONL) | Pass |
| Val/test splits | Pass |
| Version manifest | Pass |

## Commands

```bash
python -m mini.orchestrator quality --execute
python -m mini.orchestrator standardize --execute
python -m mini.orchestrator run sprint4 --execute
```

## Outputs

- `data/lake/training/standard_records.jsonl` (+ `.parquet`)
- `data/lake/validation/…`, `data/lake/test/…`
- `mini/datasets/versions/v…/manifest.json`
- `data/lake/STANDARD_LATEST.json`

## Handoff to Sprint 5

- `W-ANALYZE` coverage dashboard after each standardize cycle
