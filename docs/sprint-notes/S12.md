# Sprint 12 Notes — Instruction + Agri-QA SFT

**Status:** Complete  
**Sprint:** S12  
**Feature phase:** FP-7  
**Date:** 2026-07-19  

## Goal

Instruction-tune Mini from `v0.2-base` into **v0.3-instruct** and **v0.4-agri-qa** with multilingual QA, RAG-context conditioning, and safety refusals. Gold val token-F1 / train loss should beat base.

## Delivered

1. **`mini/models/sft_data.py`**
   - System / user / assistant format
   - Multilingual QA from lake synth
   - RAG-context “answer from context only” examples
   - Safety/refusal templates (EN/MR/HI)
   - `token_f1` / `exact_match` helpers
2. **`mini/models/sft.py`**
   - Load `v0.2-base` when present
   - Stage train: v0.3 instruct+safety → v0.4 full mix + RAG
   - Baseline vs SFT metrics, demos, `SFT_LATEST.json`
3. **`W-SFT`** ready (`mini/workers/sft.py`)
4. CLI `sft --execute --steps-v03 120 --steps-v04 120`
5. API `GET/POST /api/lake/sft`
6. Pipeline `sprint12` / `sft`

## Acceptance

| Criterion | Result |
|---|---|
| Instruction format | Pass |
| Multilingual + safety + RAG-context mix | Pass |
| Checkpoints v0.3 / v0.4 (local) | Pass |
| beats_base (F1 ↑ or loss ↓) | Pass |
| Side-by-side demos in report | Pass |

## Local-only (do not push)

- `mini/models/v0.3-instruct/**`
- `mini/models/v0.4-agri-qa/**`
- `mini/models/SFT_LATEST.json`
- `mini/models/**/*.pt`
- `data/lake/**`, `mini/datasets/**`

## Commands

```bash
python -m mini.orchestrator sft --execute --steps-v03 120 --steps-v04 120 --seed 42
python -m mini.orchestrator run sprint12 --execute
```

## Handoff to Sprint 13

- `W-EVAL` gold sets, gates, hallucination probes, scorecard for v0.4
