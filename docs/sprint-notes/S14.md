# Sprint 14 Notes — Quantize + Version Packaging

**Status:** Complete  
**Sprint:** S14  
**Feature phase:** E5-quant  
**Date:** 2026-07-19  

## Goal

Produce deployable Mini artifacts with size budgets: INT8 (and optional INT4) export, CPU latency p95, model card / license / provenance, and a local version registry (`v0.5-quant`, optional `v0.5-reasoning-lite`).

## Delivered

1. **`mini/models/quantize.py`**
   - Dynamic INT8 Linear quantization
   - INT4 weight packing for size demos
   - FP32 vs INT8 vs INT4 size comparison
   - CPU latency p50/p95
   - Budgets: INT8 ≤ 4 MiB, INT4 ≤ 2 MiB
2. **`mini/models/deploy.py`**
   - Package-only `mini/models/serve/<tag>/`
   - `MODEL_CARD.json`, `LICENSE.txt`, `PROVENANCE.json`
   - `VERSION_REGISTRY.json` + semver policy
   - Optional `v0.5-reasoning-lite` alias
3. **`W-QUANT`**, **`W-DEPLOY`** ready
4. CLI `quant` / `deploy`
5. API `GET/POST /api/lake/quant`, `/api/lake/deploy`
6. Pipeline `sprint14` = quant → deploy

## Acceptance

| Criterion | Result |
|---|---|
| INT8 export | Pass |
| INT4 optional pack | Pass |
| INT8 ≤ budget and smaller than FP32 | Pass |
| Latency p95 logged | Pass |
| Model card + license + provenance | Pass |
| Version registry | Pass |

## Local-only (do not push)

- `mini/models/v0.5-quant/**`
- `mini/models/serve/**`
- `QUANT_LATEST.json`, `DEPLOY_LATEST.json`, `VERSION_REGISTRY.json`
- Prior checkpoints `v0.2`–`v0.4`

## Commands

```bash
python -m mini.orchestrator quant --execute --version v0.4
python -m mini.orchestrator deploy --execute --version v0.4 --tag v0.5-quant --force
python -m mini.orchestrator run sprint14 --execute
```

## Handoff to Sprint 15

- `W-INFER` + wrap `W-RAG` / agents: intent → retrieve → Mini → validate
